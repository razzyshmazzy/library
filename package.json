{
  "name": "library-root",
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,client -c magenta,cyan \"npm run dev --prefix server\" \"npm run dev --prefix library\"",
    "install:all": "npm install --prefix server && npm install --prefix library"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
